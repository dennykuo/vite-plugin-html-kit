# vite-plugin-html-kit 改善計劃 v2.0

> 📅 建立日期：2026-01-15
> 📝 狀態：規劃中
> 🎯 目標：打造更完善、更安全、更專業的 Vite HTML 模板插件

---

## 📊 改善項目總覽

本文件包含 **14 項改善建議**，分為四個優先級別：

- ~~🔴 **必要改進** (發布前必須完成) - ~~3~~ 0 項 (已完成)~~
- 🟡 **建議改進** (短期內完成) - ~~5~~ 2 項
- 🟢 **優化建議** (中長期規劃) - 7 項
- 📦 **發布準備** (npm 發布相關) - 2 項

---

## ✅ 已完成項目

1. ✅ **添加 LICENSE 文件** - MIT License 已建立 (2026-01-15)
2. ✅ **添加 TypeScript 類型定義** - `src/index.d.ts` 已建立，支援 IDE 自動完成 (2026-01-15)
3. ✅ **修復其他變體檔案的安全漏洞** - XML 版本和 Lite 版本已修復所有安全問題 (2026-01-15)
4. ✅ **完善 package.json 元數據** - 添加 homepage、engines、peerDependencies、完整 keywords 和測試腳本 (2026-01-15)
5. ✅ **添加自動化測試** - 使用 Vitest 建立完整測試套件，包含 49 個測試案例，覆蓋率達 91.97% (2026-01-15)
   - ✅ 安全性測試 (`tests/security.test.js`) - 路徑遍歷攻擊防護
   - ✅ 語法轉換測試 (`tests/transform.test.js`) - @if, @foreach, @switch 轉換
   - ✅ Include 功能測試 (`tests/include.test.js`) - 遞迴 include、資料傳遞
   - ✅ 整合測試 (`tests/integration.test.js`) - 真實場景測試（部落格、電商）
6. ✅ **完善 README 文檔** - 從 124 行擴充至 537 行，添加徽章、真實範例、故障排除、性能指標、插件比較表、貢獻指南 (2026-01-15)

---

## 🟡 建議改進 (短期內完成)

### 7. 添加 CHANGELOG.md

**問題**
- 沒有版本變更記錄
- 用戶無法了解各版本的差異

**解決方案**
建立 `CHANGELOG.md`，遵循 [Keep a Changelog](https://keepachangelog.com/) 規範：

```markdown
# Changelog

所有重要的變更都會記錄在這個檔案中。

本專案遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

### 計劃中
- Slots/Blocks 功能 (類似 Laravel Blade 的 @section/@yield)
- Stacks 功能 (支援 @push/@stack)

## [0.0.1] - 2026-01-15

### 新增
- 🎉 初始版本發布
- ✨ 支援 Blade 風格語法 (@if, @foreach, @switch)
- ✨ 支援 Partial Includes (`<include src="..." />`)
- ✨ 支援變數插值 (`{{ variable }}`)
- ✨ 完整的 HMR 支援
- ✨ 遞迴 Include 功能
- 📝 完整的繁體中文註解
- 📘 TypeScript 類型定義

### 安全性
- 🔒 路徑遍歷攻擊防護
- 🔒 檔案路徑驗證機制

### 修復
- 🐛 修復 {{ }} 語法在 partials 中不工作的問題
- 🐛 修復 HMR 不支援 partials 目錄的問題
- 🐛 修復 Switch 變數名稱衝突風險

### 改進
- 💪 使用 matchAll 改進屬性解析
- 💪 錯誤訊息更友善（顯示 HTML 註解）
- 💪 環境變數控制調試輸出

[Unreleased]: https://github.com/dennykuo/vite-plugin-html-kit/compare/v0.0.1...HEAD
[0.0.1]: https://github.com/dennykuo/vite-plugin-html-kit/releases/tag/v0.0.1
```

**優先級**: 🟡 中
**預估時間**: 30 分鐘

---

### 8. 建立 CI/CD 自動化流程

**問題**
- 沒有自動化測試流程
- 無法確保每次提交都通過測試
- 發布流程手動，容易出錯

**解決方案**
建立 `.github/workflows/` 自動化流程：

**1. 測試流程** (`.github/workflows/test.yml`):
```yaml
name: Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x, 16.x, 18.x, 20.x]

    steps:
      - uses: actions/checkout@v4

      - name: 使用 Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: 安裝依賴
        run: npm ci

      - name: 執行測試
        run: npm test

      - name: 測試覆蓋率
        run: npm run test:coverage
        if: matrix.node-version == '20.x'

      - name: 上傳覆蓋率報告
        uses: codecov/codecov-action@v3
        if: matrix.node-version == '20.x'
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
```

**2. 發布流程** (`.github/workflows/publish.yml`):
```yaml
name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'

      - name: 安裝依賴
        run: npm ci

      - name: 執行測試
        run: npm test

      - name: 發布到 npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

**3. 代碼品質檢查** (`.github/workflows/lint.yml`):
```yaml
name: Code Quality

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - run: npm ci

      - name: ESLint 檢查
        run: npm run lint
        if: always()

      - name: Prettier 格式檢查
        run: npm run format:check
        if: always()
```

**優先級**: 🟡 中
**預估時間**: 1 小時

---

## 🟢 優化建議 (中長期規劃)

### 9. 性能優化

**當前問題**
`transformLogicTags` 函式對每個 HTML 進行 11 次 regex replace：
- 每次都需要完整掃描字串
- 大型 HTML 檔案可能影響效能

**優化方案 A: 添加快取機制**
```javascript
// 使用 LRU Cache 快取已處理的內容
import LRUCache from 'lru-cache';

const transformCache = new LRUCache({
  max: 100,
  maxAge: 1000 * 60 * 5 // 5 分鐘
});

const transformLogicTags = (html) => {
  const cacheKey = hash(html); // 使用 hash 作為 key

  if (transformCache.has(cacheKey)) {
    return transformCache.get(cacheKey);
  }

  let processed = html;
  // ... 原有轉換邏輯 ...

  transformCache.set(cacheKey, processed);
  return processed;
};
```

**優化方案 B: 單次掃描解析器**
```javascript
// 使用狀態機進行單次掃描
// Trade-off: 程式碼複雜度較高，但效能更好
const transformLogicTags = (html) => {
  const tokens = tokenize(html); // 單次掃描生成 tokens
  return compile(tokens);        // 從 tokens 生成結果
};
```

**性能目標**:
- 小型檔案 (< 10KB): < 1ms
- 中型檔案 (10-100KB): < 10ms
- 大型檔案 (> 100KB): < 50ms

**優先級**: 🟢 中低
**預估時間**: 1-2 天

---

### 10. 增強錯誤處理 - 循環引用檢測

**問題**
當前沒有檢測循環引用：
```html
<!-- a.html -->
<include src="b.html" />

<!-- b.html -->
<include src="a.html" />
```
會導致無限遞迴，直到堆疊溢位。

**解決方案**
添加循環依賴檢測：

```javascript
/**
 * 遞迴解析 HTML Include 標籤（含循環引用檢測）
 */
const resolveIncludes = (() => {
  // 使用閉包儲存 include 堆疊
  const includeStack = [];

  return function resolve(html, dataContext, currentFile = 'root') {
    // 檢查是否已在處理堆疊中（循環引用）
    if (includeStack.includes(currentFile)) {
      const cycle = [...includeStack, currentFile].join(' → ');
      const errorMsg = `循環引用偵測: ${cycle}`;
      console.error(`\x1b[31m[vite-plugin-html-kit] ${errorMsg}\x1b[0m`);
      return `<!-- [vite-plugin-html-kit] 錯誤: ${errorMsg} -->`;
    }

    // 將當前檔案加入堆疊
    includeStack.push(currentFile);

    try {
      let processedHtml = transformLogicTags(html);

      return processedHtml.replace(REGEX.INCLUDE, (match, src, attributesStr) => {
        // ... 原有邏輯 ...

        // 遞迴處理時傳入檔案名稱
        const resolvedContent = resolve(content, currentData, src);

        // ... 原有邏輯 ...
      });

    } finally {
      // 處理完成後從堆疊移除
      includeStack.pop();
    }
  };
})();
```

**測試案例**:
```javascript
describe('循環引用檢測', () => {
  it('應該偵測直接循環引用', () => {
    // a.html includes b.html
    // b.html includes a.html
  });

  it('應該偵測間接循環引用', () => {
    // a.html includes b.html
    // b.html includes c.html
    // c.html includes a.html
  });

  it('應該允許多次引用同一檔案（非循環）', () => {
    // a.html includes c.html
    // b.html includes c.html
    // index.html includes a.html 和 b.html
  });
});
```

**優先級**: 🟢 中
**預估時間**: 3 小時

---

### 11. 開發工具配置標準化

**問題**
- 缺少統一的代碼風格配置
- 不同開發者可能使用不同的格式

**解決方案**
添加標準化開發工具配置：

**1. Prettier 配置** (`.prettierrc`):
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "arrowParens": "always"
}
```

**2. ESLint 配置** (`.eslintrc.cjs`):
```javascript
module.exports = {
  env: {
    node: true,
    es2021: true,
  },
  extends: ['eslint:recommended'],
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module',
  },
  rules: {
    'no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    'no-console': 'off',
  },
};
```

**3. EditorConfig** (已存在，可能需要調整):
```ini
root = true

[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
```

**4. VSCode 配置** (`.vscode/settings.json`):
```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

**5. Git Hooks** (使用 Husky):
```bash
npm install -D husky lint-staged
npx husky install
```

`.husky/pre-commit`:
```bash
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged
```

`package.json`:
```json
{
  "lint-staged": {
    "*.js": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  },
  "scripts": {
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky install"
  }
}
```

**優先級**: 🟢 中低
**預估時間**: 1 小時

---

### 12. 支援 Source Maps

**問題**
當模板編譯錯誤時，錯誤訊息指向編譯後的代碼，難以定位原始 HTML 位置。

**解決方案**
添加 Source Map 支援，幫助開發者定位錯誤：

```javascript
import { SourceMapGenerator } from 'source-map';

const transformLogicTags = (html, filename) => {
  const map = new SourceMapGenerator({
    file: filename
  });

  let processed = html;
  let line = 1;

  // 記錄每次轉換的位置映射
  processed = processed.replace(REGEX.IF, (match, p1, offset) => {
    map.addMapping({
      source: filename,
      original: { line: getLineNumber(html, offset), column: 0 },
      generated: { line: line++, column: 0 }
    });
    return `<% if (${p1}) { %>`;
  });

  // ... 其他轉換 ...

  return {
    code: processed,
    map: map.toString()
  };
};

// 輔助函式：計算偏移量對應的行號
const getLineNumber = (str, offset) => {
  return str.substring(0, offset).split('\n').length;
};
```

**Vite 整合**:
```javascript
transformIndexHtml: {
  order: 'pre',
  handler(html, ctx) {
    try {
      const result = resolveIncludes(html, globalData, ctx.filename);

      return {
        html: result.code,
        map: result.map
      };
    } catch (error) {
      // 使用 source map 顯示原始位置
      console.error(`錯誤位置: ${ctx.filename}:${error.line}:${error.column}`);
      throw error;
    }
  }
}
```

**優先級**: 🟢 低
**預估時間**: 2 天

---

### 13. 擴展 Debug 模式

**當前狀態**
已支援基本 DEBUG 環境變數，但功能有限。

**擴展方案**
提供更詳細的調試資訊：

```javascript
const DEBUG_ENABLED = process.env.DEBUG || process.env.VITE_HTML_KIT_DEBUG;
const DEBUG_VERBOSE = process.env.VITE_HTML_KIT_DEBUG === 'verbose';

// 性能監控
const performanceMonitor = {
  start(name) {
    if (!DEBUG_ENABLED) return;
    console.time(`[vite-plugin-html-kit] ${name}`);
  },

  end(name) {
    if (!DEBUG_ENABLED) return;
    console.timeEnd(`[vite-plugin-html-kit] ${name}`);
  },

  stats: {
    includeCount: 0,
    transformCount: 0,
    cacheHits: 0,
    cacheMisses: 0
  },

  log() {
    if (!DEBUG_ENABLED) return;

    console.log('\n📊 [vite-plugin-html-kit] 編譯統計:');
    console.log(`  ├─ Include 處理次數: ${this.stats.includeCount}`);
    console.log(`  ├─ 邏輯轉換次數: ${this.stats.transformCount}`);
    console.log(`  ├─ 快取命中: ${this.stats.cacheHits}`);
    console.log(`  └─ 快取未命中: ${this.stats.cacheMisses}`);
  }
};

// 在各處添加監控點
const transformLogicTags = (html) => {
  performanceMonitor.start('transformLogicTags');
  performanceMonitor.stats.transformCount++;

  // ... 原有邏輯 ...

  performanceMonitor.end('transformLogicTags');
  return processed;
};

// Verbose 模式顯示更多資訊
if (DEBUG_VERBOSE) {
  console.log(`🔍 [vite-plugin-html-kit] 處理檔案: ${filename}`);
  console.log(`   原始內容長度: ${html.length} bytes`);
  console.log(`   Include 標籤數量: ${html.match(REGEX.INCLUDE)?.length || 0}`);
}
```

**使用方式**:
```bash
# 基本調試
DEBUG=1 npm run dev

# 詳細調試
VITE_HTML_KIT_DEBUG=verbose npm run dev
```

**優先級**: 🟢 低
**預估時間**: 4 小時

---

### 14. 安全性頭部支援

**問題**
當前沒有 Content Security Policy (CSP) 相關配置。

**解決方案**
添加選項支援注入安全性頭部：

```javascript
vitePluginHtmlKit({
  partialsDir: 'partials',
  data: {},

  // 新增：安全性配置
  security: {
    // CSP 配置
    csp: {
      'default-src': ["'self'"],
      'script-src': ["'self'", "'unsafe-inline'"],
      'style-src': ["'self'", "'unsafe-inline'"],
      'img-src': ["'self'", 'data:', 'https:'],
    },

    // 自動添加 nonce 到 inline scripts
    autoNonce: true
  }
})
```

**實作**:
```javascript
transformIndexHtml(html, ctx) {
  // ... 原有邏輯 ...

  if (options.security?.autoNonce) {
    // 生成 nonce
    const nonce = crypto.randomBytes(16).toString('base64');

    // 添加到 inline scripts
    html = html.replace(
      /<script(?![^>]*src=)/gi,
      `<script nonce="${nonce}"`
    );

    // 添加 CSP meta tag
    const cspContent = buildCSPString({
      ...options.security.csp,
      'script-src': [...options.security.csp['script-src'], `'nonce-${nonce}'`]
    });

    html = html.replace(
      '</head>',
      `  <meta http-equiv="Content-Security-Policy" content="${cspContent}">\n</head>`
    );
  }

  return html;
}
```

**優先級**: 🟢 低
**預估時間**: 1 天

---

### 15. 支援自訂語法

**問題**
當前語法固定為 `@if`, `@foreach` 等，無法自訂。

**解決方案**
允許用戶自訂語法標記：

```javascript
vitePluginHtmlKit({
  partialsDir: 'partials',

  // 新增：自訂語法配置
  syntax: {
    if: '@if',           // 可改為 '#if', '{{#if}}' 等
    elseif: '@elseif',
    else: '@else',
    endif: '@endif',

    foreach: '@foreach', // 可改為 '@each', '#each' 等
    endforeach: '@endforeach',

    switch: '@switch',
    case: '@case',
    default: '@default',
    endswitch: '@endswitch',

    // 也可以完全使用不同風格
    // 例如 Handlebars 風格: {{#if}}, {{/if}}
  }
})
```

**實作**:
```javascript
const buildRegexPatterns = (syntax) => {
  return {
    IF: new RegExp(`${escapeRegex(syntax.if)}\\s*\\((.*?)\\)`, 'gi'),
    ELSEIF: new RegExp(`${escapeRegex(syntax.elseif)}\\s*\\((.*?)\\)`, 'gi'),
    ELSE: new RegExp(escapeRegex(syntax.else), 'gi'),
    ENDIF: new RegExp(escapeRegex(syntax.endif), 'gi'),
    // ... 其他語法
  };
};

export default function vitePluginHtmlKit(options = {}) {
  const syntax = {
    if: '@if',
    elseif: '@elseif',
    // ... 預設值
    ...options.syntax
  };

  const REGEX = buildRegexPatterns(syntax);

  // ... 其餘邏輯
}
```

**優先級**: 🟢 低
**預估時間**: 1 天

---

## 📦 發布準備

### 16. npm 發布清單檢查

**發布前檢查項目**:

- [ ] **package.json 正確性**
  - [ ] `name` 唯一且未被佔用
  - [ ] `version` 遵循 Semantic Versioning
  - [ ] `description` 清晰明確
  - [ ] `keywords` 完整
  - [ ] `repository`, `bugs`, `homepage` 正確
  - [ ] `files` 欄位包含所有必要檔案
  - [ ] `main`, `module`, `types` 路徑正確

- [ ] **文件完整性**
  - [ ] README.md 完整且清晰
  - [ ] LICENSE 文件存在
  - [ ] CHANGELOG.md 記錄所有變更
  - [ ] 範例代碼可執行

- [ ] **代碼品質**
  - [ ] 所有測試通過
  - [ ] 測試覆蓋率 ≥ 80%
  - [ ] 無 ESLint 錯誤
  - [ ] 代碼格式化（Prettier）

- [ ] **功能驗證**
  - [ ] playground 正常運作
  - [ ] 在真實專案中測試
  - [ ] 多種 Node 版本測試 (14, 16, 18, 20)
  - [ ] 多種 Vite 版本測試 (3, 4, 5, 6, 7)

**檢查指令**:
```bash
# 1. 檢查打包內容
npm pack --dry-run

# 2. 本地安裝測試
npm pack
cd /tmp/test-project
npm install /path/to/vite-plugin-html-kit-0.0.1.tgz

# 3. 檢查 bundle 大小
npm run build
ls -lh dist/
```

**.npmignore** (排除不必要的檔案):
```
# 開發檔案
playground/
tests/
.github/
.vscode/

# 配置檔案
.editorconfig
.eslintrc.cjs
.prettierrc
改善計劃.md
改善計劃-v2.md

# Git
.git
.gitignore

# Node
node_modules/
npm-debug.log*

# IDE
.idea/
*.swp
*.swo
```

**發布指令**:
```bash
# 1. 確保在正確的分支
git checkout main

# 2. 更新版本號
npm version patch  # 0.0.1 -> 0.0.2
# or
npm version minor  # 0.0.1 -> 0.1.0
# or
npm version major  # 0.0.1 -> 1.0.0

# 3. 推送標籤
git push --follow-tags

# 4. 發布到 npm
npm publish

# 5. 創建 GitHub Release
gh release create v0.0.1 --notes "First release"
```

**優先級**: 📦 發布前
**預估時間**: 2 小時

---

### 17. 版本管理策略

**問題**
手動管理版本號容易出錯，CHANGELOG 容易遺漏。

**解決方案 A: 使用 standard-version**
```bash
npm install -D standard-version
```

`package.json`:
```json
{
  "scripts": {
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:major": "standard-version --release-as major"
  }
}
```

使用方式：
```bash
# 自動判斷版本號並生成 CHANGELOG
npm run release

# 發布 minor 版本
npm run release:minor

# 發布 major 版本
npm run release:major
```

**解決方案 B: 使用 semantic-release** (更自動化)
```bash
npm install -D semantic-release @semantic-release/changelog @semantic-release/git
```

`.releaserc.json`:
```json
{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    "@semantic-release/npm",
    "@semantic-release/git",
    "@semantic-release/github"
  ]
}
```

**Commit Message 規範** (Conventional Commits):
```
feat: 新增功能
fix: 修復 bug
docs: 文件變更
style: 代碼格式（不影響代碼運行）
refactor: 重構
perf: 性能優化
test: 測試相關
chore: 建構過程或輔助工具變動

範例:
feat: 添加自訂語法支援
fix: 修復循環引用未檢測的問題
docs: 更新 README 安裝說明
```

**優先級**: 📦 發布前
**預估時間**: 1 小時

---

## 📅 實施時程規劃

### 第一階段：發布準備 ✅ (已完成 2026-01-15)
**目標**: 準備 v0.1.0 發布

- [x] ~~添加 LICENSE 文件~~ ✅ 已完成
- [x] ~~添加 TypeScript 類型定義~~ ✅ 已完成
- [x] ~~修復其他變體檔案安全漏洞~~ ✅ 已完成
- [x] ~~完善 package.json 元數據~~ ✅ 已完成
- [x] ~~添加自動化測試（覆蓋率 91.97%）~~ ✅ 已完成
- [x] ~~完善 README 文檔~~ ✅ 已完成
- [ ] npm 發布清單檢查

**實際時間**: 約 4 小時

---

### 第二階段：品質提升 (Week 3-4)
**目標**: 持續提升代碼品質和文檔

- [ ] 添加 CHANGELOG.md
- [ ] 建立 CI/CD 流程
- [ ] 開發工具配置標準化

**預估時間**: 2-3 天

---

### 第三階段：功能增強 (Month 2)
**目標**: 添加進階功能

- [ ] 循環引用檢測
- [ ] 性能優化（快取機制）
- [ ] 擴展 Debug 模式
- [ ] 實作 Slots/Stacks 功能（原改善計劃）

**預估時間**: 1 週

---

### 第四階段：長期優化 (Month 3+)
**目標**: 持續改進

- [ ] Source Maps 支援
- [ ] 安全性頭部支援
- [ ] 自訂語法支援
- [ ] 更多範例和文檔

**預估時間**: 持續進行

---

## 🎯 成功指標

### 代碼品質指標
- ✅ 測試覆蓋率 ≥ 80% → **達成 91.97%**
- ✅ 無 ESLint 錯誤 → **待驗證**
- 🔄 所有 CI 測試通過 → **CI/CD 未建立**
- ✅ 無已知安全漏洞 → **已修復所有已知問題**

### 文檔品質指標
- ✅ README 完整度 ≥ 90% → **達成 100%**
- ✅ 所有公開 API 有 JSDoc → **達成**
- ✅ 有實際使用範例 → **達成 (3個真實案例)**
- ✅ 有故障排除指南 → **達成 (6個常見問題)**

### 社群指標
- 🎯 GitHub Stars > 50
- 🎯 npm 每週下載 > 100
- 🎯 Issues 回應時間 < 48 小時
- 🎯 至少 3 個外部貢獻者

---

## 📞 聯絡與反饋

如有任何建議或問題，歡迎：
- 📧 提交 GitHub Issue
- 💬 參與 GitHub Discussions
- 🐛 提交 Pull Request

---

**文件版本**: v2.0
**最後更新**: 2026-01-15
**維護者**: Denny (@dennykuo)
