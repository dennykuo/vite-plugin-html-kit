# 功能擴充 (Advanced Features)

- Slot / Block 支援:
類似 Laravel Blade 的 @section 和 @yield，或是 Vue 的 <slot>。
這允許外層樣板定義結構，內層 partial 填入具體內容（反向控制）。

- Stack 支援:
類似 Laravel Blade 的 @push('scripts') 和 @stack('scripts')。
允許在深層 partial 中定義 <script>，然後統一推送到 <head> 或 <body> 底部輸出。